Remote Desktop Desktop release.

Included assets:

- macOS installer (`.dmg`)
- Windows installer (`.msi`)

Notes:

- Installers are currently unsigned.
- macOS Gatekeeper and Windows SmartScreen may show warnings until code signing is added.
- Web viewer, relay, and host agent are not bundled by this workflow.
