# az-codex-desktop

`az-codex-desktop` is a Dioxus desktop shell that mirrors the Codex desktop layout at scaffold level.

Run it with:

```shell
cargo run --manifest-path apps/codex/desktop/Cargo.toml
```

The current skeleton includes a left navigation rail, a bottom settings entry, and a central placeholder content area.
