# Keep real credentials out of git.
# Paste your Neon PostgreSQL URL into one of these env vars before running.
TOASTY_DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require&channel_binding=require
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require&channel_binding=require
