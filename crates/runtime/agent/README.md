# az-agent

General AI agent runtime library.

This crate is intentionally scaffolded with the agent runtime dependencies first. Public agent APIs should be added as focused modules under `src/` and collected from the crate root.
